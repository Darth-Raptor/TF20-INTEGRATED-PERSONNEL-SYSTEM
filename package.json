{
  "name": "tf20-website-portal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Task Force 20 clean-slate PMS backend planning and data-model foundation.",
  "scripts": {
    "check": "node --check prisma/seed.mjs && node --check scripts/check-area1-model.mjs && node --check scripts/check-area2-access.mjs && node --check scripts/check-area3-workflows.mjs && node scripts/check-area1-model.mjs && node scripts/check-area2-access.mjs && node scripts/check-area3-workflows.mjs",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy",
    "prisma:dev": "prisma migrate dev",
    "db:seed": "node prisma/seed.mjs"
  },
  "engines": {
    "node": ">=18.20.0"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0"
  },
  "prisma": {
    "seed": "node prisma/seed.mjs"
  }
}
